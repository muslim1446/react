{
  "name": "tuwa-web-player",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wrangler:dev": "npm run build && wrangler pages dev ../_site",
    "deploy": "npm run build && wrangler pages deploy ../_site --project-name=tuwa-player"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0",
    "wrangler": "^3.0.0"
  }
}
